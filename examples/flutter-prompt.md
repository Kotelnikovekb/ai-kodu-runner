# Flutter implementation task

Implement the requested feature in this Flutter project.

Before changing code:

1. Inspect the existing architecture and follow its conventions.
2. Use Context7 for current Flutter and package documentation when an API or package version is uncertain.
3. Read and follow all available project skills under `.opencode/skills/` or `.agents/skills/`.

After changing code:

1. Run `flutter analyze`.
2. Run formatting checks.
3. Run unit and widget tests.
4. Do not commit or push changes.
5. Leave a concise summary in the final response.
